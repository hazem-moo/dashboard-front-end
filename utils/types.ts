export type PropsHeader = {
  project_title: string;
  description: string;
};

export type getOrders = {
  title: string;
  category: string;
  price: number;
  count: number;
  discount: number;
};
